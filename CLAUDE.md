# Monster Versus — заметки для Claude

Браузерный пошаговый пиксельный рогалик (по мотивам MicroMonsters). Vite + TypeScript + DOM/CSS, без фреймворка, тесты Vitest. Весь код, комментарии, тексты игры и документация — на русском.

Что читать по задаче:
- `README.md` — запуск, все debug-параметры URL (`?hero=…&seed=…&phase=…`), объект `mv` в консоли.
- `docs/GDD.md` — дизайн-документ: числа героев/врагов/предметов, правила боя, §8 «Экраны и интерфейс» (раскладка кадра, плитки, оверлеи, клавиши), §12 «Принятые решения», §13 «Статус» (история версий с цифрами бота).
- `docs/adr/` — разборы отложенных решений: что выяснено, какие варианты рассмотрены и почему пока не делаем. Сейчас там `0001-obshchie-sidy.md` (почему один сид не даёт одинаковый забег и как это чинить) и `0002-sunduki-posle-zabega.md` (как были устроены сундуки за забег и как их вернуть).
- `docs/statistika.md` — статистика забегов: что уходит в Google Таблицу, когда не уходит, как подключить и проверить таблицу.
- `docs/art.md` — как готовятся изображения: конвейер «мастер в `art/` → скрипт в `tools/` → рабочий файл в `src/assets/` → строка в таблице кода» и общие правила (палитра, размеры под FullHD, вес, сглаживание против pixelated), потом три трека — листы кадров героев (раскладки grid/scattered, клипы по ролям, манифест cell/body), аватарки и фоны локаций (мастер 3:1 → два кадра 960×320) с командами и тем, что просить у генератора; в конце промпт STYLE + FRAME + CHARACTER (STYLE и FRAME не редактировать — это и есть единый стиль), чек-листы приёмки и раскладка файлов.
- Этот файл — карта кода, рабочие соглашения и ловушки.

## Команды

```bash
npm run dev                                          # http://localhost:5173
npx vitest run                                       # тесты движка (~3 с, 296 тестов)
node node_modules/typescript/bin/tsc --noEmit -p .   # typecheck — ТОЛЬКО так (см. ловушки)
node node_modules/vite/bin/vite.js build             # сборка в dist/
SIM=1 npx vitest run tests/balance-sim.test.ts       # бот-симулятор баланса, все герои, 60 забегов
SIM=1 SIM_HERO=berserk SIM_N=300 npx vitest run tests/balance-sim.test.ts   # один герой; 300 забегов на всех — ~15 с
SIM=1 SIM_SIG=2 SIM_N=300 npx vitest run tests/balance-sim.test.ts          # все герои со вторым персональным артефактом (SIM_SIG=1 — с первым, по умолчанию); каждый артефакт пары держит героя в коридоре отдельно
SIM=1 SIM_LOCS=caves,caves,caves SIM_N=200 npx vitest run tests/balance-sim.test.ts   # один босс на всех трёх актах (замер фаз босса)
SIM_DEBUG=1 SIM_HERO=warrior SIM_SEED=3 SIM_FIGHTS=4 npx vitest run tests/sim-debug.test.ts   # лог первых боёв бота
SIM_DEBUG=stall SIM_HERO=paladin npx vitest run tests/sim-debug.test.ts                     # найти пат
SIM=1 SIM_N=150 npx vitest run tests/sim-profile.test.ts                                   # профиль бота в метриках статистики игрока: ходы и урон на бой по актам, тиры сборки в победах
SIM_DEATH=L2R10 SIM_HERO=warrior SIM_SHOW=2 npx vitest run tests/sim-death.test.ts         # гибели бота в клетке: сборка и хвост лога последнего боя
SIM=1 SIM_N=200 npx vitest run tests/gnome-odds.test.ts                                     # как часто бот успевает убить вора на снаряжении каждого акта
```

Перед коммитом: tsc из node_modules + `npx vitest run`. После правки любых чисел баланса — SIM на 300 забегах.

## Карта кода

```
src/engine/   чистая логика, без DOM, покрыта тестами
  types.ts      ВСЕ типы + SAVE_VERSION, GAME_VERSION (версия в углу меню), MAX_ENEMIES, MAX_ALLIES. Начинать чтение отсюда.
  rng.ts        mulberry32: createRng/next/int/chance/pick/shuffle/weighted
  stats.ts      computeStats: база героя → кубик оружия (владение) → перки → аффиксы → пассивки
  combat.ts     бой: статусы, урон, союзники, действия героя, ИИ врагов, createBattle, describeAction/computeIntent (ActionInfo.marks — pierce/drain для пилюли, v0.40.2); дальность (SUMMON_FRONT, reachableEnemies/actionReach/canReach, REACH_ERR, удар плетью по ряду, эффекты pull и push)
  equipment.ts  слоты и типы сокетов (SlotKind: weapon | armor | any; slotKindAt/slotAccepts/canPlaceArtifact/freeSocketFor), addArtifact/replaceArtifact/equipGear, апгрейд дубликатом
  loot.ts       константы экономики (золото, цены, шанс зелья, цена кузнеца) и генерация наград/магазина/событий (rollEventKind); rollRewards(…, focus) катит тройку из пула RewardFocus (attack — оружие и оружейные артефакты, defense — броня и бронные; focusGearKind) закреплённым составом REWARD_ROLES и перемешивает её; rollArtifact(…, slot) сужает пул по типу сокета и катит взвешенно (artifactWeight: DUPLICATE_WEIGHT, SYNERGY_WEIGHT и manaWeight по maxMp для приёмов с costsMana; заводки/выплаты — artifactStatuses/heroApplies/heroPaysFor)
  run.ts        машина состояний забега: enterRoom → startEvent | startBattle → finishBattle (лог боя → run.logs с battleTitle) → reward (за бой и элиту экран создаётся с пустыми options и ждёт chooseRewardFocus — слепой выбор пула, v0.39; awaitsFocus/canChooseFocus; canReroll до выбора — «Сначала выберите пул», skipReward можно) → advanceRoom …; события: takeChest, altarPray/altarSacrifice, forgeUpgrade, gnomeTakeLoot, leaveEvent; бой с вором закрывают finishGnome/finishSnatcher (фаза `event` с итогом)
  report.ts     runReport(run, ctx) — запись статистики забега (RunReport: плоское поле = колонка таблицы, detail — JSON со снаряжением и боями); чистая, тесты в run.test.ts
  globalStats.ts summarize(feed, opts) — сводка по ответу таблицы ?data=runs для экрана «Статистика» (забеги/победы по героям, средний победный забег, урон, топ «клетка — убийца» и топ убийц, чем заканчивали: базы оружия/брони и артефакты); чистая, tests/globalStats.test.ts
src/data/     типизированные таблицы; каждый файл экспортирует list-based Record + xxxDef(id)
  heroes.ts     6 героев (warrior, mage, assassin, paladin, berserk, archer), weaponSkill/armorSkill — владение оружием и бронёй (два состояния), signatures — пара персональных артефактов (первый открыт всегда, второй после победы героем), defaultSignature, SIGNATURE_OWNER (оба id → герой)
  enemies.ts    69 врагов по локациям (секции ═══; два гнома-вора числятся за лесом, но приходят событием любого акта), хелпер act(), ai: cycle | boss-rules; блок у 44 из 69 (v0.38: райдер `block` к приёму 16 врагов); evade — процентный уворот с порога, spriteScale — размер спрайта; фазы боссов: phase2 + условия p1/p2 в правилах, второе тело через onDeath: summon + aura
  artifacts.ts  65 артефактов: пассивные / активные физ. (STA) / активные маг. (MP), тиры 1–3 через t(a,b,c); 12 персональных (пара на героя, v0.33); reach — своя дальность приёма; slot — тип (weapon 38 / armor 27; Ответный удар бронный с v0.39.1, Мазь знахаря добавлена в v0.40.2), в какой сокет встаёт; связки v0.38 (секции «Связки»): выплаты по статусам (detonate/spread/breakBlock/finisher) и пассивки, читающие цель; у приёма могут быть mods (Оглушающий удар)
  gear.ts       16 баз оружия + 8 брони (одна startOnly), перки (заводки в перках: дротики — кровь, праща — оглушение), типы, дальность базы (reach: копьё any; плеть row — её перк «Хлёст» даёт sweep, SWEEP_MULT), weaponReach(gear, def), владение (canWieldWeapon/canWearArmor — перк базы работает только у владеющего, чужое оружие ещё и UNSKILLED_DICE_MULT), вес оружия (heft → HEFT_MULT), DEF/HP брони по типу (ARMOR_TYPE_STATS), аффиксы (8 оружейных, из них три стихийные заводки v0.38.11: onHitBleed/onHitBurn/onHitPoison — рана с каждого удара, обрабатываются в heroStrike), makeGear, upgradeGearTier (кузнец), типы сокетов (rollSlotKinds, SLOT_ANY_CHANCE 0.4), makeStartingGear — сигнатура в предмет своего типа
  potions.ts    7 зелий, без тира
  locations.ts  6 локаций с encounters, ACTS (тиры лута; rareGear — редкий тир с шансом в обычном пуле акта, v0.35: акт 2 тир 4 на 5 %), ACT_SCALE, ACT_DMG_BONUS, ACT_TOUGH_HP (надбавка к HP элит и боссов по акту, v0.37.1: +15 % в первом), ROOM_KINDS, EVENT_WEIGHTS, BOSS_HEAL_PCT
  collection.ts каталог коллекции — собирается автоматически из данных выше; ключи находок (у артефактов с тиром: `art:fireball@2`), loadoutFinds — что у героя в руках, foundState/collectibleLines — что открыто
src/ui/       рендер и клики
  app.ts        class App: state (run, screen, armed — выбранный приём, aim — цель по Tab, busy, logOpen, sheetOpen, pauseOpen…), render() диспатчит по screen/run.phase
                и добавляет оверлеи, commit() = saveRun + render, ход врагов с таймером ENEMY_STEP_MS (под оверлеем ждёт), таймер забега
  frame.ts      runFrame(app, parts): топбар 40 + центр 320 + консоль 180 — все шесть экранов забега
  topbar.ts     кнопка «Персонаж» (☻), золото, акт/локация, лента комнат (ROOM_ICONS), ход, таймер, меню; console.ts — блок героя, hubGear, кнопка лога
  screens/*.ts  один экран — одна функция xxxScreen(app): HTMLElement; heroSheet.ts и pause.ts — оверлеи; runLog.ts — тело лога за весь забег (runLogBody: прошлые бои свёрнуты, текущий строками) и оверлей лога вне боя; bestiary.ts — альбом врагов по локациям (describeAction из combat.ts); stats.ts — «Статистика»: главные вкладки app.statsScope (all — все игроки с вкладками app.statsTab: герои / гибели / убийцы / оружие / броня / артефакты; mine — своя из профиля), каждая на весь кадр; app.statsFeed из fetchRuns (App.showStats перезапрашивает на каждом заходе, прошлая таблица висит до ответа, в шапке «обновляем…» / «не обновилось»), statsLoading/statsError, &mock=1 → statsMock
  components.ts карточки предметов, бары, чипы, иконки типов, pendingModal; gearTile.ts — плитка экипировки с сокетами 2×2; dom.ts — h()/button()
  tooltip.ts    свои подсказки: атрибуты tip / tipTitle в h() → data-tip; keywords.ts — подсветка ключевых слов в описаниях
  preview.ts    ридаут, подсветка целей (ok/far) и штриховка предпросмотра урона в бою (пишет в DOM без перерисовки, покой — по app.armed); diff.ts — дельты к надетому в карточках
  fx.ts         типовые анимации боя: planHeroFx/planEnemyFx (план до применения действия), playShots (снаряды на старом поле, возвращает impact), playAfter/eventFx (облако дебафа, свечение бафа, щит блока перед бойцом, глоток); спрайты снарядов процедурные
  hotkeys.ts    1–9, Space, C, L, Esc; на экране пула награды 1/2 — Нападение/Защита
  sprites.ts, backgrounds.ts, icons.ts   процедурная пиксель-графика (data URL) врагов и союзников; фоны локаций рисованные у всех шести — BACKGROUNDS в backgrounds.ts (кадры 960×320 в src/assets/backgrounds/, режет tools/location-bg.py; процедурных фонов нет с v0.41.1, лежат в истории); в icons.ts, кроме иконок статусов, метки удара markIcon('pierce' | 'drain') — тот же шаблон 8×8 с автоконтуром
  heroSprite.ts рисованные герои: лист на героя src/assets/heroes/<id>.png (ряд — клип, 8 кадров; у Воина и Мага по семь клипов, у остальных только покой; клипы названы по роли (attack/heavy/power/heal/block/hurt/death), чего нет — подменяется по цепочке FALLBACK), манифест HERO_SHEETS (cell, body — печатает tools/hero-sheet.py), heroSprite(id, px, base) и playHeroClip; кадры листает CSS .hero-sprite. Там же heroAvatar(id, px) — рисованный портрет src/assets/heroes/<id>-avatar.png 256×256 (консоль 44, плитка выбора 112, шапка листа 80); в бою, на выборе крупно и на итогах — спрайт. SpriteSpec героя в данных остаётся ради цвета клинка в fx.ts
  tint.ts       тонировка бойцов под свет локации (v0.41.5): TINTS — цвет света и сила на локацию, фильтр feColorMatrix на каждую кладётся в документ один раз, поле боя отдаёт выбранный спрайтам через переменную --tint (style.css: `.field .sprite-wrap img.sprite` и `.field .hero-sprite::before`). Цвет меряет tools/location-tint.py по кадру фона; отладка — `&tint=off` и `&tint=<сила>&tintc=<цвет>`
  preload.ts    прогрев картинок: warmImages(...адреса) заказывает файл браузеру заранее через new Image(), повторы отсекает Set; зовёт App.warmArt() из render() — фон текущей локации (оба кадра) и файлы героя, с клетки 9 ещё и фон следующей локации. Своего кэша у файлов нет: их держит браузер, а data URL процедурной графики — Map в sprites.ts/icons.ts/fx.ts
  save.ts       localStorage: забег (mv_run_v1, сброс при смене SAVE_VERSION) и профиль (mv_profile_v1, переживает версии; статистика, коллекция, бестиарий — recordFinds/recordEnemies из App.render()); playerId — анонимный id для статистики; sigPick/unlocks и signatureUnlocked/pickedSignature — выбор и открытие второго персонального артефакта (heroWins[id] > 0 или отладочный mv.unlockAll())
  telemetry.ts  reportRun(run, event, profile): STATS_URL (адрес веб-приложения Apps Script, пусто — не шлём), отсечка localhost/домашней сети (кроме &stats=1), пометка debug, fetch no-cors keepalive; fetchRuns() — GET ?data=runs для экрана «Статистика», читается и с localhost; своего кэша нет с v0.40.5 (запрос на каждом заходе, кэширует сам Apps Script), dropRunsCache() чистит ключ mv_runs_v1 прежних версий
src/main.ts   монтирование, масштаб кадра 960×540, разбор debug-параметров URL
src/style.css один файл, секции /* ─── … */
tests/        vitest; sim/bot.ts — умный бот (W — веса оценки, planTurn, playRun, chooseReward; chooseFocus — пул награды по сокетам и тиру предмета, вес W.focusSocket)
tools/hero-sheet.py         сборка листа героя из картинки генератора (numpy, Pillow, scipy): `python tools/hero-sheet.py art/mage.png --hero mage --clips idle,battle,attack,power,block,hurt,death` (лист с ячейками) или `--layout scattered --heroes ,mage,... --clip idle` (фигуры россыпью; пустое имя — пропустить ряд). Сырой лист НЕ резать по сетке руками; исходники — в art/. Клипы называются по роли (attack/power), а не по рисунку в листе
tools/location-bg.py        два кадра фона локации из мастера генератора (Pillow): `python tools/location-bg.py art/forest-bg.png --loc forest --zoom 1.6 --center 0.36` — wide (вся сцена, поле боя) и tall (кусок покрупнее по земле, карта и хабы), оба 960×320: в логическом кадре 1:1, на FullHD ×2, pixelated ничего не пересчитывает. Сам срезает чёрные кромки генератора и берёт кадр 3:1 от низа; `--ground <px>` дотягивает низкую полосу пола до 75 px (бойцы стоят на 38 и 64 px над низом кадра) — растягивает нижнюю полосу вниз и срезает сверху столько же; зумы, центры и --ground всех шести локаций — в docs/art.md
tools/location-tint.py      цвет света локации для тонировки бойцов (numpy, Pillow): `python tools/location-tint.py --loc caves` — медиана цветных пикселей кадра `-wide`, нормированная по максимуму канала, плюс подсказка силы по насыщенности; печатает готовую строку для `TINTS` и адрес для проверки в бою. Подсказка, а не приговор: цвет доводится на скриншоте (`&tint=`/`&tintc=`)
tools/hero-avatars.py       аватарки героев из листа портретов (numpy, Pillow): `python tools/hero-avatars.py art/heroes-avatars.png --heroes warrior,mage,assassin,paladin,berserk,archer` — сетку ячеек находит сам по тёмным промежуткам, порядок чтения = порядок героев
tools/apps-script/Code.gs   приёмник статистики: отдельный проект с SPREADSHEET_ID (таблица «Monster Versus Metrics» в Drive пользователя), лист runs, колонки по ключам записи (новый ключ — новая колонка справа); GET ?data=runs отдаёт последние 2000 без отладочных и без player/detail
```

Ключевые инварианты:
- Движок мутирует `RunState`/`BattleState` на месте и не трогает DOM. UI вызывает `R.xxx(run)` и `commit()`.
- Вся случайность — через `run.rng` / переданный `Rng`. Один сид + герой = тот же забег. Не использовать `Math.random` в движке.
- UI боя (v0.26, «гибрид»): сначала приём, потом цель. `App.armed` — ключ выбранного приёма ('attack' или id артефакта), `actionSpecs(app)` в battle.ts отдаёт все плитки с `targeted`, `action(target)`, `targets` и `preview(target?)`; клик по плитке с целью — `app.arm(key)`, по врагу — `app.applyArmed(uid)`, приёмы без цели применяются сразу. `settleArmed()` в render() снимает выбор, когда приём стал неприменим (стамина, перезарядка, конец боя), `endTurn` — всегда. Подсветка `ok`/`far` на врагах — из `app.armedTargets()` при отрисовке, наведение на плитку перекрашивает временно (`PreviewSpec.targets`), `clearPreview` возвращает покой. Постоянной цели (`app.target`) нет; отладочные `&use=` бьют первого врага.
- UI: наведение (ридаут, штриховка, дельты в плитке) пишет прямо в готовые узлы через preview.ts/diff.ts и ничего не хранит в App — любое действие перерисует экран. `App.render()` считает отпечаток экрана (screen, phase, клетка, число наград, pending, ожидание пула награды) и при его смене глотает клики `SETTLE_MS` (400 мс) в capture-фазе на root — иначе второй клик двойного клика по «Надеть» в награде нажимал «Войти» на карте; в Playwright-скриптах после перехода ждать ≥ 400 мс перед кликом. Второе исключение — слой `.fx-layer` в поле боя: fx.ts вешает в него снаряды и облака, а `App.render()` переносит живой слой в новое дерево, чтобы полёт доигрался.
- Анимации боя (fx.ts): `App.battleAction` считает план до мутации, применяет действие, играет снаряды и ждёт `impact` мс (`fxTimer`, действия в это время не принимаются), потом render + playEvents. Род анимации выводится из эффектов и типа оружия, переопределение и цвет — поле `fx: FxSpec` у ArtifactDef/PotionDef/Base оружия/EnemyAction. Рядовые враги только наскакивают (`acting`), `fx` ставить лишь элите и боссам. Дебаф → облако, блок → щит, баф/лечение → свечение — из событий боя, в данных не задаются; приём на себя с эффектом block тоже даёт щит. Отладочные циклы в main.ts зовут `battleAction(action, false)` — без ожидания. Многоударный приём (v0.35): движок кладёт по событию `damage` на удар, `playEvents` играет цифры одной цели по очереди с шагом `HIT_GAP` (fx.ts, 200 мс) и на каждый удар после первого дёргает `lungeAgain` бьющего, возвращает хвост в мс — `scheduleStep(extra)` ждёт его перед следующим врагом; у элит со снарядом `planEnemyFx` ставит взмах на каждый удар, а `impact` — по первому. v0.40.2: взмахи по числу ударов появились и у героя (`addSwings` в fx.ts — по эффекту-удару приёма, ×2 при «Эхо удара»), а полоска HP ползёт вместе с цифрами: `planDrains` в app.ts собирает по цели её удары пакета, читает итог прямо из `.bar-text` и `setBarHp` отматывает полоску к состоянию после первого удара (без перехода, иначе она поедет вверх) и опускает по куску на удар. Цель с лечением в том же пакете пропускается — счёт собьётся. Кнопки, у которых при недоступности нужен ридаут, делаются без атрибута `disabled` (браузер не шлёт им наведение), а с классом `off`.
- Лог боя: `log()` в combat.ts пишет всё — раскладку урона героя (`heroAttackDamage` возвращает `why`), судьбу удара через `HitDetail`/`hitTail` (уязвимость, кольчуга, блок, промахи), каждое `addStatus`, `gainBlock` с источником, `healHero`/`healEnemy` с источником. Новые механики обязаны логировать себя. `BattleState.roster` — имена врагов на старте; `finishBattle` кладёт лог в `run.logs` (`BattleLog`, `result: won | lost | fled` — бегство вора не победа). Одна кнопка «Лог боя» / `L` на любом экране: в бою панель на поле, вне боя оверлей (`logOpen`, `toggleLog`).
- Вторая фаза босса: `EnemyDef.phase2` (порог доли HP, эффекты перехода, аура) — `checkPhases` в combat.ts проверяет порог после каждого пакета урона (вызов в начале `cleanupDead`), переход происходит в ход героя, ставит `EnemyState.phase = 2` и `aura`, шлёт событие `phase` и заново выбирает намерение. Правила ИИ различают фазы условиями `p1`/`p2`; приём «тот же, но другой» во второй фазе — отдельный id (перезарядка считается по id). Переход стоит боссу ближайшего хода (v0.36): `checkPhases` ставит `e.intent = PHASE_SHIFT` (enemies.ts) и чистит `forcedNext`, `enemyAction` собирает из `phase2` синтетический приём с эффектами `phase2.guard` (стража перехода: блок, `dodge`, `invuln` или `thorns` с `turns` — шипы врага на срок появились здесь), `computeIntent` подписывает его «не атакует» плюс стража, `actEnemy` логирует «без атаки», `App.isPhaseShift` не даёт наскока; бот видит эффекты приёма сам. Ловушка: `chooseIntent` при переходе больше не зовётся, поэтому RNG-поток забега отличается от v0.35 — тест «бессмертный герой» получил в `playRun` ещё и урон 50, иначе простой бот с чужим оружием на половине кубика вечно бился с Осиной королевой. Тела после смерти (Лич, Капитан) — обычный `onDeath: summon` на враг с полем `aura`. UI: `.enemy.aura` с `--aura`, вспышка `phaseBurst` в fx.ts по событию `phase`. Бестиарий показывает числа первого акта (`firstActScale` в bestiary.ts, тот же `enemyScale`).
- Типы сокетов (v0.31): `GearInstance.slotKinds: SlotKind[]` параллельно `slots` (`'weapon' | 'armor' | 'any'`), у артефакта `ArtifactDef.slot: 'weapon' | 'armor'`. Проверка только в `canPlaceArtifact` / `slotAccepts` (equipment.ts): `addArtifact` берёт свободный подходящий сокет (`freeSocketFor` — свой тип раньше универсального), `replaceArtifact` бросает на неподходящий, `pendingPlace` возвращает false (UI зовёт `canPendingPlace` и красит кнопку `off`), вытесненный «Заменить» артефакт встаёт в конец `pending.artifacts` (`displaced` — его id для заголовка модалки), после первой вставки `cancellable = false`; бот вытесненный артефакт никем не заменяет (только свободный сокет или выброс): ценности зависят от статов и после перестановки меняются — пара артефактов вытесняла друг друга без конца, паты Воина 1 → 67, `equipGear` расселяет по типам (свои сокеты первыми, потом универсальные, остальное в overflow). `slotKindAt` считает сокет без записи универсальным — так живут тесты со `slotKinds: []` и отладочный `&art=`. Генерация — `rollSlotKinds` в makeGear/upgradeGearTier, у оружия не бывает бронных сокетов, у брони оружейных. Прямая запись `slots[i] = …` в тестах и `mkBattle` тип не проверяет — это по замыслу. Бот: `weakestSocket(run, forId)` и `artifactGain` смотрят только подходящие сокеты, `gearGain` считает потерянные артефакты через `equipGear` на копии героя. Новый артефакт обязан нести `slot`.
- `canXxx(run)` возвращает `string | null` (причина запрета или null), парный `xxx(run)` возвращает boolean/void. UI показывает причину в подсказке.
- Дальность (v0.26): ближний бой достаёт только `state.enemies[0]`; `reachAny` и `sweep` в DerivedStats — свойства типа оружия и базы (`Base.reach`: копьё `any`, плеть `row`), владение не участвует; `ArtifactDef.reach` — своя дальность приёма (без поля: `school: 'magic'` — любая цель, физический — как оружие). Проверка — `canReach`/`canUseAction` (`REACH_ERR` «Только первый в ряду»; отказ Крюка «Уже первый в ряду» снят в v0.40.3 — с кровью приём осмыслен и по первому, `pull` там просто ничего не двигает). Базовый удар плетью бьёт всех на `SWEEP_MULT` без сквозного удара, приёмы плетью — первого; `sweep` приходит из перка «Хлёст», у не владеющего плеть бьёт первого. Маркер дальности на карточках и плитках — `reachDots(gear, def)` в components.ts (три точки, ближнее красит одну). Призванные и отделившиеся встают вперёд (`SUMMON_FRONT`, unshift в `placeEnemy`), союзники дальности не держат. Новый приём по врагу обязан решить свою дальность (щит, порез, Молот света — в упор; склянка, крюк — любая), а тест, который бьёт второго врага ближним оружием, упадёт с этой причиной. Эффекты `pull` и `push` двигают цель в начало `enemies` и на клетку назад — порядок массива и есть ряд.
- Персональные артефакты (v0.33): у героя пара `HeroDef.signatures`, выбранный лежит в `HeroPersistent.signature` — от него зависят `makeStartingGear(def, sig)`, `canDropFor` (невыбранный из пары в этом забеге не выпадает никому), ×2 в `artifactValue` бота и колонка `signature` в статистике. `newRun(heroId, seed, now, signature)` бросает на чужой id; `App.newRun` чужой id из `&sig=` молча заменяет выбором из профиля. Открытие второго — `signatureUnlocked` в save.ts (`heroWins` или `unlocks`), UI выбора — `signatureCard` в heroSelect.ts. Бот в SIM гоняется с `SIM_SIG=1|2`. Новые механики пары: стат `riposte` («Ответный удар» Воина, v0.34 вместо «Натиска»: в `damageHero`, когда блок погасил удар, ударивший получает `riposteDamage(h)` — доля среднего урона оружия с Силой, без кубика, усталости, крита и уворота; `EnemyState.riposted` сбрасывается в начале `actEnemy` — один ответ каждому врагу за его ход; шипы врага об ответ не ранят), статы `lowHpStr`/`lowHpSta`/`lowHpReduce` («Боевой транс» — Сила, стамина и гашение удара, пока HP ниже `TRANCE_HP_PCT` = ½ с v0.33.2; сила в бою — `heroStr(h)`, а не `stats.str`, бот считает её в `dpt` через `tranceStr`, гашение — `tranceReduce(h)` в `damageHero` и в `projectIncoming` бота), скрытность спадает после **каждого** бьющего эффекта приёма, а не после всего приёма (Двойной выпад: в спину бьёт только первый удар; Вихрь одним эффектом по всем — по-прежнему целиком из тени).
- Дроп под сборку (v0.40): тройка награды собирается по ролям `REWARD_ROLES` — предмет, артефакт, свободная карточка с `ARTIFACT_CHANCE`, — и перемешивается `shuffle`, так что без артефакта или без предмета награда не приходит (`rollArtifact` вернул null — вместо артефакта катится предмет). `rollArtifact` катит `weighted`, а не `pick`: `artifactWeight` даёт ×`DUPLICATE_WEIGHT` (3) за артефакт, уже стоящий в сокете, и ×`SYNERGY_WEIGHT` (3) за вторую половину связки — обе тяги перемножаются. Связки читаются из данных: `artifactStatuses(id)` (кэш на id, разбор по тиру 3 — на первом часть статов ещё нули) собирает, какие статусы артефакт вешает на врага и на каких у него выплата, `heroApplies`/`heroPaysFor` — то же по герою, причём заводки из аффиксов и перков снаряжения берутся из `computeStats`. **Новый стат выплаты по статусу обязан попасть в `artifactStatuses`** (эффекты `status`, `enchant`, `detonate`, `spread`, `spell.vsWeak` и статы `vsBleed`, `dotLeech`, `poisonVuln`, `spellVsBurn`, `stunCrit`, `perDebuff`, `onHit*`, `markOnHit`, `stunOnCrit`) — иначе связка есть в бою, но дроп её не видит. Веса ничего не запрещают, кроме маны (ниже).
- Заклинания по мане (v0.40.1): третий множитель в `artifactWeight` — `manaWeight(stats.maxMp)` для всего, у чего `costsMana(id)` (цена MP хоть на одном тире; ключ — цена, а не `school`, из-за Молота света: `physical` при `{ sta: 1, mp: 1 }`). Ступени 0 / 0.5 / 1 / 2 по порогам 0, ≤2, ≤4, выше. Ноль означает ноль: безмановому герою приёмы с ценой MP не выпадают совсем, поэтому `rollArtifact` считает сумму весов и при нуле берёт `pick` — иначе `weighted` бросит. Максимум маны берётся из `computeStats`, а не из `HeroDef`, так что перк оружия, аффикс брони и Кристалл маны открывают заклинания тому, кто в ману вложился.
- Пул награды (v0.39): после боя и элиты `run.rewards[0]` создаётся с `options: []` и без `focus` — `awaitsFocus(screen)` истинно, UI рисует две карточки пула, `takeReward` и `rerollReward` молчат, `skipReward` работает. `chooseRewardFocus(run, focus)` ставит `focus` и катит тройку `rollRewards(…, focus)`: предметы только этого типа, артефакты только с `slot` этого типа (пул пуст — предмет). Бот и простой бот тестов обязаны выбирать пул, иначе цикл наград виснет (`playRun` в run.test.ts чередует по клетке, `sim/bot.ts` — `chooseFocus`). Босс (`bossGear`/`bossArt`) и зелье пула не ждут. Отладочный `&phase=reward` без `&focus=` останавливается на экране пула.
- Забег: 3 акта × 10 клеток `['fight','fight','event','fight','fight','event','fight','elite','event','boss']`, после босса лечение `BOSS_HEAL_PCT` и сразу следующая локация (привала между актами нет). Клетка `event` при входе разыгрывает `EventKind` по `EVENT_WEIGHTS` (camp 10, elite 5, shop 22, chest 22, altar 22, forge 9, gnome 5, gnome_art 5); `run.event` хранит, что выпало (элита из события даёт золото и награду как клетка элиты — `effectiveRoomKind`). Локации 3 из 6 случайно без повторов, ярусов у локаций нет (решение пользователя: все равноправны и ротируются); `tier` локации — только справочный «родной» акт, под который записаны числа врагов, `enemyScale` приводит их к фактическому акту.
- Фазы забега: `map | battle | reward | shop | event | camp | victory | defeat`; `shop` и `camp` — тоже из события; фаза `event` — сундук, алтарь, кузнец и итог боя с вором (по `run.event.kind`; `gnome`/`gnome_art` при входе сразу начинают бой, `result: fight | fled | slain`). `run.pending` — артефакт ждёт выбора слота (обрабатывать до всего остального). После боя может быть 2 экрана награды (второй — зелье), поэтому в тестах и ботах награды пропускать циклом `while (run.phase === 'reward')`. В тестах событие нужного вида — `startEvent(run, kind)` после `run.roomIndex = 2`.
- Вор (v0.29): статус `evade` — процентный уворот врага, бросок в `damageEnemy` только для `hit`/`spell` и только если в `HitOpts` передан `rng` — новый прямой удар героя/союзника без `rng` уворот молча не заметит (раны и шипы бьют всегда — это по замыслу). Промах пишется в `HitDetail.miss` («уворот 38 %»). Эффекты врага `stealGold`/`stealArtifact` копят добычу в `BattleState.stolen`/`stolenArtifact` (артефакт уходит из `hero.artifacts` сразу, `stripArtifactMods` снимает пассивку и чистит кулдаун), `flee` убирает врага с поля живым: `phase: 'won'` + `fled`, убитым не считается; `turnsToFlee` — часики над плиткой. Бот ценит кражу и побег в `projectIncoming` (`W.goldHp`, `W.stolenArt`) и делит HP врага на (1 − уворот) в `evaluate`.
- Связки (v0.38): чистого «+N» к урону в пуле больше нет (убраны Камень силы, Мощный удар, Тяжёлый наконечник, Око мудреца) — новый артефакт должен либо читать состояние цели, либо сцепляться с другим действием. Эффекты: `detonate` (сила × оставшиеся ходы ран → урон `dot`-типом мимо блока, статус снят; `pooled` — сумма со всех бьёт каждого), `spread` (копия статусов с цели на остальных), `breakBlock` (блок цели → урон, `canUseAction` «У цели нет блока»), `finisher` (`per × hero.attacks`, «Сначала атакуйте», атакой не считается), `attack.refundOnKill`, `spell.vsWeak`. Статы, зависящие от цели, считаются в `vsTargetBonus` (heroAttackDamage и `previewAttack(…, target)`), крит наверняка — `sureCritOn` (тень или `stunCrit` по оглушённому); `poisonVuln` — свой множитель в `damageEnemy`/`previewOnTarget` поверх уязвимости; `spellVsBurn` — в `applyEffect` 'spell' (плюс продление горения); эффект `chain` (Цепная атака — активный приём по одному заряду: любой другой приём ставит `hero.uses['_chain_ready'] = 1`, цепная атака — 0, заряд не копится; `chainCharges(h)`, «Сначала примените приём»), `spellSta`/`skillMp` (ключи `_cross_*` в `hero.uses`) и статус `echo` (повтор атаки или бьющих оружием эффектов приёма с той же усталостью; что именно повторяется — `ECHO_EFFECTS` / `echoesEffect` в combat.ts, с v0.40.4 туда входят `finisher`, `blockStrike`, `breakBlock` и `chain`, заклинания — нет; тот же список читает `planEffects` в fx.ts, чтобы взмахов было столько же, сколько ударов) — в `performAction`; `dotLeech` — в `actEnemy` на тике; эффект `enchant` (Стихийная заточка, v0.38.7): статус `enchant` на герое с полем `Status.element` (стихия бросается `pick(rng, ENCHANT_ELEMENTS)` при наложении, `addStatus(..., element)`; повтор меняет стихию), рана вешается в `heroStrike` рядом с `onHitBleed`; бот в `heroApplies` считает заточку заводкой всех трёх ран. UI: `rangeOn(target)` в battle.ts даёт разброс по цели, штриховка взрыва и пролома — `kind: 'dot'` (без блока). Бот: `staValue` (цена стамины с усталостью), `heroApplies`/`heroPaysFor` (заводка ↔ выплата), `modsValue` общий для пассивок и приёмов с модами.
- Срок статуса (`turns`) по умолчанию сгорает в конце хода владельца и считает его собственные ходы («Слабость на 2 хода» = две порченые атаки). Статусы из `OPPONENT_PHASE` (combat.ts: `stealth`, `invuln`) работают в чужой ход, поэтому тикают в начале следующего своего хода, уже отработав ход противника: `tickDurations(c, 'start' | 'end')`, для героя — из `startPlayerTurn` и `endTurn`, для врага — в начале и в конце `actEnemy`. Новый статус, который гасит чужие удары, добавлять в `OPPONENT_PHASE`, иначе наложенный в свой же ход он потеряет ход впустую (так «завеса на 2 хода» прикрывала один ход врага до v0.24). Аналитическим прикидкам (бот) вместо `turns > 1` использовать `holdsThroughEnemyTurn(status)`. Повторное наложение: сила складывается у `STACKING`, срок — у `STACKING_TURNS` (`stealth`, v0.32.3: шашка поверх Тени покрова продлевает тень), у остальных и то и другое по максимуму. Что-то, что вешается до `startPlayerTurn` (createBattle), ставить после него.
- Счётчики хода героя (v0.40.2): `hero.attacks` — число атакующих **действий** (усталость `fatigue ** attacks`, бонус первого удара, `markOnHit`), `hero.strikes` — число **ударов** (растёт в `applyEffect` на каждый эффект `attack` и в `performAction` на каждый замах базовой атаки, включая эхо). Финишер и его запрет «Сначала атакуйте» считают `strikes`, всё остальное — `attacks`. Новый приём из нескольких ударов получает это даром, новый бьющий тип эффекта — только если пройдёт через `applyEffect`.
- Рана-довесок приёма врага (v0.40.2): `applyEnemyEffect` получает из `actEnemy` контекст приёма `EnemyActionCtx` (`attacked`/`landed`), и `debuff` с DoT-статусом не ложится, если удар этого же приёма весь прошёл мимо (`detail.miss`). Блок промахом не считается, удар по союзнику — тоже; `selfDestruct` проверяет это сам. Приём врага без удара вешает статусы как раньше.
- Бой: STA — очки действий, полностью в начале хода; MP — только реген в начале хода, полностью после комнаты; блок сгорает в начале хода (кроме `blockKeep`); каждая следующая атака в ходу слабее в `fatigue` раз; «Защититься» даёт `ceil(DEF × DEFEND_MULT)`. Скрытность (Дымовая шашка, Тень покрова): проверять через `isHidden(h)`; праща — `stunOnCrit` (шанс оглушить, бросок только после критического удара). Крит (v0.21): пара статов — `crit` (шанс 0..1) и `critDmg` (проценты обычного урона, база у каждого героя своя, минимум 100); снаряжение прибавляет проценты (`critDmg: 40`), а не множит. Крит-статы сверх пары: `critRamp` («Азарт», копит `hero.critStack`, крит обнуляет), `executeCrit` («Клеймо палача», цель ниже `EXECUTE_HP_PCT`), `critHeal` («Жажда крови»). Статус `vulnerable`: `VULNERABLE_MULT` 1.25 к ударам и заклинаниям в `damageEnemy`/`damageHero`/`previewOnTarget`, округление `Math.round` (вниз — съедает бонус при уроне 3–7). Новые статы `onKillHeal`, `blockStart`, `markOnHit`. Эффект `attack.blockPct` — доля урона замаха (dmg, не дошедшего до HP) в блок героя (Щитовой удар); эффект `blockStrike` — урон = текущий блок × mult как удар без кубика, усталости и крита, `canUseAction` даёт «Нет блока» (Таран). Лимит применений за ход: `ArtifactDef.usesPerTurn`, счётчик `hero.uses` (сброс в `startPlayerTurn`, проверка в `canUseAction`).
- Статистика забегов (v0.27, момент отправки переделан в v0.30.2): исход уходит, когда он решён, — гибель шлёт `App.noteOutcome()` из `render()` по `battle.phase === 'lost'`, ещё до кнопки «К итогам», победу — `App.afterPhaseChange` до `recordResult` (в записи — число законченных забегов «до этого»); `App.dropRun` — брошенный забег (из `newRun` и `abandonRun`), а погибший, из которого ушли в меню, — как `defeat`. Одну запись на забег держит `RunState.reported`: отметка лежит в сохранении (`App.report()` сохраняет забег перед отправкой), поэтому перезагрузка не задваивает, а забег, закрытый на плашке гибели, досылается при следующем запуске. `runReport` подмешивает незакрытый бой (цифры, `lastBattle`, последняя запись `detail.battles` с исходом `lost` или `unfinished`). `RunState.debug` ставит `App.newRun(…, true)` из `?hero=` в main.ts; `debug` в записи = `run.debug || localhost`. Новое поле — в `RunReport` (report.ts), тест в run.test.ts и таблицу в docs/statistika.md; скрипт таблицы колонку добавит сам. Отладочный `phase=end` забег не заканчивает и ничего не шлёт.

## Как добавлять контент

- **Враг**: запись в `enemies.ts` (секция локации, `act(...)`, `ai`), добавить в `encounters` локации в `locations.ts`, строка в таблице GDD §7. Спрайт — `blob(...)` или `humanoid(...)`. Второе тело босса (`onDeath: summon`) в `encounters` не добавлять — оно появляется само, в бестиарии числится отдельной записью локации.
- **Фаза босса**: `phase2` у босса, новые приёмы с отдельными id и условиями `p1`/`p2` в правилах; проверять `SIM_LOCS=<loc>,<loc>,<loc>` до/после (бот плохо переносит уворот, шипы и многоударные приёмы с Силой — первые версии роняли его до 12–24 %). Тест — рядом с «вожак на половине HP» в `combat.test.ts`.
- **Артефакт**: `artifacts.ts` в нужной секции, обязательно `slot` (оружейный — бьёт/растит урон, бронный — HP/ресурсы/блок/лечение; тест на 38/26 в equipment.test.ts поправить; новый стат пассивки — ещё и вес в `modsValue` бота, иначе он её не берёт; выплата по статусу — ещё и в `heroPaysFor` бота и в `artifactStatuses` в loot.ts, иначе дроп не сведёт связку; заводка подхватится сама из эффектов `status`); новый `Effect`/стат — сначала в `types.ts` (`Effect`, `DerivedStats`), обработка в `combat.ts`, дефолт в `stats.ts`, описание в GDD §5. Коллекция подхватит сам.
- **База оружия/брони**: `gear.ts` `WEAPON_BASES`/`ARMOR_BASES` с перком через `byTier([...])`; оружию — `heft` (light/heavy или ничего) и `spread`, броне — `armorType` (он же задаёт DEF/HP через `ARMOR_TYPE_STATS`); GDD §4. Новые статы перка — как у артефактов.
- **Герой**: `heroes.ts` (weaponSkill, armorSkill, стартовое снаряжение, `signatures` — пара персональных артефактов, выбранный стоит в предмете своего типа, второй сокет пуст), GDD §3.2, потом SIM на нём с `SIM_SIG=1` и `SIM_SIG=2`. Мана героя теперь управляет дропом (v0.40.1): при `mp: 0` приёмы с ценой MP ему не выпадают вовсе, поэтому **сигнатура с ценой MP у малованового героя будет глушиться множителем** — либо мана, либо цена в STA. Персональные артефакты фильтрует `canDropFor` в loot.ts; в UI чип/карточка получают класс `signature`.
- **Изменил форму `RunState`/`BattleState`/`HeroPersistent`** — поднять `SAVE_VERSION` в `types.ts` (старые сейвы просто сбрасываются, миграций нет).
- Тесты: `combat.test.ts` (`mkBattle(hero, [enemies])` фиксирует урон на среднем, крит 0 и ставит героям классическую пару артефактов `LEGACY_PAIR`, не стартовую), `run.test.ts` (простой `playBattle`, `resolvePending`), `weapons.test.ts`, `armor.test.ts`. Новая механика — новый `it` рядом с похожим.
- После фичи обновить: `README.md` (если новые параметры/команды), GDD — таблицы + новый абзац `vX.Y:` в §13 и при необходимости пункт в §12.

## Как менять UI (v0.11, гибрид «топбар + консоль»)

- Экран забега = `runFrame(app, { cls, center, top?, mid, right?, log?, overlays? })`: центр 320 px, `top` — строка во всю ширину правее блока героя (в бою ридаут), `mid` — центр консоли под ней (в бою плитки, на хабах `hubGear(app)`), `right` — блок 110 px, `overlays` — модалки. Топбар и блок героя экран не рисует — они общие. Экраны вне забега (меню, выбор героя, коллекция, бестиарий, итоги) каркас не используют.
- Оверлеи «Персонаж» и «Пауза» — состояние `app.sheetOpen` / `app.pauseOpen`, рисуются в `App.render()` поверх экрана; ничего не вставлять в DOM мимо `render()`, кроме наведения (ниже).
- Подсказки: только атрибуты `tip` / `tipTitle` в `h()` и `button()`; нативный `title` не использовать (две подсказки). Описания предметов пропускать через `markKeywords()`.
- Наведение в бою: `bindPreview(app, el, () => spec)` из `preview.ts` — ридаут `.readout` и штриховка `.bar-ghost` на полоске цели. Пишет в готовые узлы, ничего не хранит. На хабах предпросмотра при наведении нет (убран по просьбе пользователя): дельты к надетому — только в карточке (`gearDiffLines` из `diff.ts`).
- Плитки боя: `TileSpec` в `battle.ts`; крупное число — `effectValue()`, число артефакта в сокете — `artifactShort()` в `gearTile.ts`. Ряды: класс `.tiles.rows-1|rows-2` ставит рендер по числу плиток (два ряда с восьми).
- Хоткеи в `hotkeys.ts` работают только при `app.screen === 'run'` и не в полях ввода; новые клавиши — туда же и в подпись паузы. В бою: цифра выбирает приём (повтор — применяет), Enter применяет к цели по Tab или первой применимой (`applyAim`), Tab — `aimNext()` (класс `aim` и предпросмотр без перерисовки, `app.aim` сбрасывает render), Esc сначала снимает выбор приёма.
- Клип героя для скриншота: анимация встанет, если задать спрайту ряд и остановить её — `const e=document.querySelector('.hero-zone .hero-sprite'); e.style.setProperty('--row','2'); e.classList.add('once'); e.getAnimations({subtree:true}).forEach(a=>{a.pause(); a.currentTime=<мс>})`. Ряды — порядок clips в HERO_SHEETS.
- Размеры кадра фиксированы (960×540, консоль 180, блок героя 205, плитка 78): новая строка в блок героя или плитку — сначала посчитать высоту, потом проверить скриншотом.
- Проверка: dev-сервер пользователя обычно уже крутится на :5173 (свой не поднимать). Скриншот: `msedge.exe --headless=new --disable-gpu --hide-scrollbars --window-size=960,540 --virtual-time-budget=6000 --screenshot=<png> "http://localhost:5173/?hero=warrior&seed=5&enter=1"`, PNG смотреть Read-тулом. Обязательные адреса: бой с 9 плитками `?hero=warrior&art=whirlwind,stun_strike,war_cry,second_wind,bleed_cut,fireball&enter=1`, союзники `?hero=mage&art=wolf_whistle&use=wolf_whistle&enter=1`, `&phase=reward|shop|event|camp`, события `&phase=event&event=chest|altar|forge`, трофей босса `&room=9&phase=reward`, вторая фаза босса `&locs=forest,swamp,crypt&room=9&enter=1&boss2=1` (аура; `caves` — с блоком и шипами, `crypt`/`ship` — второе тело), очки сверх максимума `&art=second_wind&use=second_wind&enter=1`, `&sheet=1`, `&pause=1`. Наведение и полный забег — временным блоком в конце `main.ts` (диспатч `mouseenter`, автоигра через методы App), затем `git checkout src/main.ts`. Расширение Claude in Chrome у пользователя не работает.
- Скриншот посреди анимации: headless-браузер запускает её с задержкой ~70 мс на клик, поэтому кадры по таймеру врут. Надёжно — Playwright: после клика `document.getAnimations().forEach(a => a.pause())`, выставить `currentTime` анимациям элементов `.fx` и снять кадр (скрипт в scratchpad, глобальный `playwright` в /opt/node22).
- После правок UI: README (управление, параметры) и GDD §8.

## Баланс

v0.40.5 (Взрыв пламени ×1/1.25/1.5 вместо ×1/1.5/2, Огненная склянка 5 + Горение 3 на 2 хода вместо 8) на 300, v0.40.4 → нерф взрыва → плюс зелье, первая сигнатура: Воин 49.3 → 49.3 → 47.7 %, Маг 52.0 → 48.7 → 49.7, Ассасин 51.3 → 49.7 → 52.3, Паладин 59.7 → 60.3 → 57.7, Берсерк 59.3 → 59.3 → 59.0, Лучник 45.3 → 47.0 → 46.7; вторая: 44.0 → 43.3 → 41.0 / 58.0 → 56.3 → 56.7 / 42.0 → 43.3 → 41.0 / 55.3 → 56.0 → 56.0 / 57.7 → 57.7 → 58.3 / 47.3 → 46.3 → 46.7. Итого среднее −0.7, крайние −3.0 и +1.3. Нерф взрыва двинул за шум только Мага ① (−3.3) — он один собирает огонь; склянка дала ещё около −1 в среднем (мгновенные 8 по всем бот ценит выше отложенных 11). Оба замера промежуточные — файлы `sim-burst-*` и `sim-v405-*` в scratchpad сессии.

v0.40.4 («Эхо удара» повторяет `finisher`, `blockStrike`, `breakBlock` и `chain`, а не только `attack`) на 300, v0.40.3 → v0.40.4: первая сигнатура 49.3 / 52.0 / 51.3 / 59.7 / 59.3 / 45.3 — совпало с базой ровно, вторая 44.0 / 58.0 / 42.0 / 55.3 / 57.7 / 47.3 — только Паладин +0.3. Правка не трогает поток RNG, а эхо в одной руке с Финишером или Тараном боту почти не выпадает; проверяется тестами в `combat.test.ts`, а не симулятором.

v0.40.3 (Крюк-кошка: Кровотечение 2/3/4 на 2 хода поверх притягивания, отказ «Уже первый в ряду» снят) на 300, v0.40.2 → v0.40.3, первая сигнатура: Воин 50.0 → 49.3 %, Маг 50.3 → 52.0, Ассасин 53.3 → 51.3, Паладин 58.0 → 59.7, Берсерк 61.3 → 59.3, Лучник 46.0 → 45.3; вторая: 47.0 → 44.0 / 57.3 → 58.0 / 38.3 → 42.0 / 53.7 → 55.0 / 60.3 → 57.7 / 46.3 → 47.3. Среднее −0.1, крайние −3.0 и +3.7 — всё в шуме: бот крюк почти не берёт (так было и в v0.26), правка для живого игрока. Заводка подхватилась сама — `artifactStatuses` в loot.ts и `heroApplies` бота читают эффекты `status` из данных, руками ничего не добавляли.

v0.40.2 (шесть мелких правок: Финишер считает удары, Пиявка пьёт с обеих ран, бронная «Мазь знахаря» против ран, уклонение уносит рану-довесок, метки удара на пилюле, полоска HP по ударам) на 300, v0.40.1 → v0.40.2, первая сигнатура: Воин 47.0 → 50.0 %, Маг 52.7 → 50.3, Ассасин 53.3 → 53.3, Паладин 51.7 → 58.0, Берсерк 60.0 → 61.3, Лучник 49.3 → 46.0; вторая: 39.0 → 47.0 / 59.7 → 57.3 / 35.3 → 38.3 / 53.3 → 53.7 / 62.3 → 60.3 / 50.7 → 46.3. Среднее +0.6, крайние −4.3 и +8.0 — пакет нейтрален. Крупные отклонения читать как перетасовку, а не как эффект правки: новый артефакт в пуле сдвигает поток RNG на каждой награде, поэтому забеги расходятся с базой целиком. **Пятая версия подряд выше коридора 20–40 (46.0–61.3), баланс не трогали (решение пользователя, ждёт плейтеста).** База сошлась с записанной в v0.40.1 до десятой доли — сравнение честное.

v0.40.1 (дроп заклинаний по мане: ступени 0 / 0.5 / 1 / 2 по `maxMp`) на 300, v0.40 → v0.40.1, первая сигнатура: Воин 48.0 → 47.0 %, Маг 38.3 → 52.7, Ассасин 52.0 → 53.3, Паладин 50.3 → 51.7, Берсерк 49.3 → 60.0, Лучник 45.7 → 49.3; вторая: 40.0 → 39.0 / 50.3 → 59.7 / 36.7 → 35.3 / 49.0 → 53.3 / 58.0 → 62.3 / 49.7 → 50.7. Маг +14.3 и +9.3 (цель правки), Берсерк +10.7 и +4.3 (побочно: ему перестали показывать мёртвые карточки — пятую часть его артефактных предложений), остальные в шуме. Разброс 35.3–62.3 при коридоре 20–40 — **четвёртая версия подряд выше, баланс не трогали (решение пользователя, ждёт плейтеста)**. Рычаг на всех сразу — `ACT_DMG_BONUS` (1.2 / 1.4 / 1.54); точечно — числа Ярости и Волшебной стрелы, поднятые в v0.38.

v0.40 (дроп под сборку: закреплённый состав тройки и веса за дубликат и связку) на 300, v0.39.1 → v0.40, первая сигнатура: Воин 49.0 → 48.0 %, Маг 37.0 → 38.3, Ассасин 47.0 → 52.0, Паладин 44.7 → 50.3, Берсерк 43.7 → 49.3, Лучник 44.7 → 45.7; вторая: 38.0 → 40.0 / 46.0 → 50.3 / 41.0 → 36.7 / 47.0 → 49.0 / 53.7 → 58.0 / 44.3 → 49.7. Плюс 2.6 пункта в среднем, крайние −4.3 и +5.7 — почти всё в шуме ±5, но знак положительный у девяти замеров из двенадцати: бот раньше доводит свои артефакты до тира 3. В коридоре 20–40 остались трое (Маг ①, Воин ②, Ассасин ②) — **баланс не трогали (решение пользователя, ждёт плейтеста)**, как после v0.37 и v0.39. Правка целилась не в бота: он и до неё не брал мусорные артефакты, а живой игрок собирал горсть несвязанных приёмов.

Единственный источник истины — умный бот (`tests/sim/bot.ts`), цель **20–40 % побед за каждого героя** на 300 забегах (с v0.30.1, решение пользователя); с v0.33 герой меряется дважды — с каждым персональным артефактом пары (`SIM_SIG=1|2`), и каждый должен держать его в коридоре сам. Коридор опускали дважды и по одной причине: живой игрок играет заметно лучше бота, поэтому его планка должна быть ниже. Было 40–60 % до v0.26, 30–50 % до v0.30.1 — **исторические записи ниже и в GDD §13 сравнивают с теми коридорами, а не с нынешним**, так что «ниже коридора» в старых абзацах читать с поправкой на их время. Живой игрок при 50 % бота сложность чувствует; при 90 % — нет.

Рычаги, которые реально работают на бота (измерено в v0.10): урон врагов `ACT_DMG_BONUS` (locations.ts) и доля DEF в блоке `DEFEND_MULT` (combat.ts). Почти не работают: лечение, частота лута, цены, усталость. HP врагов +40 % даёт паты (бот вечно защищается). Нерфы отдельных приёмов (Вихрь и т. п.) — по наводке пользователя, симулятор их не видит.

v0.39 (пул награды «Нападение» / «Защита», бот выбирает пул в `chooseFocus`) на 300, до → после, первая сигнатура: Воин 44.3 → 49.0 %, Маг 34.0 → 37.0, Ассасин 37.3 → 47.0, Паладин 39.0 → 44.7, Берсерк 34.3 → 43.7, Лучник 41.3 → 44.7; вторая: 36.7 → 34.3 / 38.0 → 46.0 / 33.3 → 41.0 / 45.0 → 47.0 / 43.3 → 53.7 / 44.7 → 44.3. Механика стоит примерно как второй переброс на каждой награде: +3…+10 почти всем, девять замеров из двенадцати выше коридора — **баланс не трогали (решение пользователя, ждёт плейтеста)**; живой игрок планирует сокеты лучше бота и выиграет от пула больше. Как бот выбирает (60 забегов, доля «Нападения» по актам): Воин 51 / 87 / 64 %, Маг 44 / 67 / 49, Ассасин 68 / 63 / 54, Паладин 49 / 80 / 71, Берсерк 31 / 17 / 47, Лучник 55 / 81 / 68 — во втором акте почти все гонятся за оружием тира 3.

v0.39.1 (Ответный удар — бронный) на 300, Воин ②: 34.3 → 38.0 %, в шуме; свободный оружейный сокет на старте бот заполняет первой находкой.

v0.38 (связки вместо прямого урона: минус четыре «+N», четыре переписаны в условные, четырнадцать новых, блок у 16 врагов, бот ценит приёмы с усталостью и синергию) на 300, v0.37.1 → v0.38, первая сигнатура: Воин 39.3 → 34.0 %, Маг 44.7 → 42.0, Ассасин 38.3 → 34.7, Паладин 39.3 → 31.0, Берсерк 31.7 → 21.0, Лучник 47.7 → 35.0; вторая: 35.0 → 29.3 / 37.3 → 34.0 / 34.7 → 27.3 / 39.7 → 39.0 / 39.7 → 40.3 / 49.0 → 32.7. Разложение (первая, 300): без блока врагов 35.3 / 45.7 / 37.3 / 37.7 / 23.0 / 40.7 — блок стоит 1–7 пунктов; со старым ботом 31.7 / 41.3 / 31.3 / 36.0 / 25.7 / 38.3 — новый бот в шуме. Все в коридоре 20–40, кроме Мага ① (42) у верхней кромки; Берсерк ① на нижней (21, гибели на боссе первого акта 57 → 66 из 300). Бот берёт заводки (Оглушающий удар, Сеть, Сглаз, Флакон) в 2–3 раза чаще, выплаты почти не берёт — они стоят ему 15–20 % без заводки в руках, а выпадают отдельно; связки — для живого игрока, коридор перепроверить после плейтеста. Компенсация по краям (решение пользователя): Ярость +2/3/4 Силы, Волшебная стрела 2/3/4 — Берсерк ① 21.0 → 34.3, Маг ① 42.0 → 33.3, вторые сигнатуры без изменений (невыбранная сигнатура не выпадает); Прицельный выстрел КД 2 → 4 — Лучник ① 35.0 → 33.3. v0.38.4: `ACT_DMG_BONUS` первого акта 1.25 → 1.2 — main → 1.2 на 300 (①): 29.3 → 34.3 / 33.7 → 35.3 / 35.0 → 35.3 / 33.7 → 37.0 / 28.7 → 30.7 / 43.3 → 43.3, в шуме; Лучник ① выше коридора ещё на main. v0.38.10: `ACT_DMG_BONUS` третьего акта 1.6 → 1.54 (итог ×3.0 рядовым) — на 300 (①): 40.0 / 33.0 / 39.0 / 38.3 / 31.3 / 37.7, все в коридоре. v0.38.11: три стихийных аффикса оружия — на 300 (①): 44.3 / 34.0 / 37.3 / 39.0 / 34.3 / 41.3, плюс 1–4 всем, Воин и Лучник выше 40. Лаборатория в облаке: копии `src`+`tests` в scratchpad, `cp` одного файла из базы (enemies.ts / bot.ts) вместо `sed`.

v0.37.1 (элита и босс первого акта +15 % HP через `ACT_TOUGH_HP`, Кровопускание `usesPerTurn` 1/2/2) на 600, v0.37 → v0.37.1: Воин 43.3 → 41.7 %, Маг 45.5 → 43.8, Ассасин 38.3 → 35.8, Паладин 37.7 → 39.5, Берсерк 32.2 → 31.8, Лучник 54.3 → 46.8; гибели на боссе первого акта из 600 забегов: Воин 34 → 54, Маг 50 → 63, Ассасин 68 → 96, Паладин 61 → 77, Берсерк 90 → 119, Лучник 36 → 51. Надбавка HP элит и боссов первого акта мерялась отдельно на 600 (база → +15 → +25 → +40 %): Воин 43.3 → 41.0 → 39.3 → 38.5, Маг 45.5 → 44.2 → 41.5 → 41.0, Ассасин 38.3 → 36.0 → 33.2 → 36.0, Паладин 37.7 → 39.8 → 36.3 → 33.5, Берсерк 32.2 → 32.0 → 27.8 → 29.3, Лучник 54.3 → 46.8 → 48.2 → 47.2 — **рычаг слабый и немонотонный** (3–5 пунктов даже на +40 %, между +25 и +40 шум), потому что элита и босс — 2 боя из 21; зато гибели на боссе R10 первого акта растут вдвое, а паты почти нет (1 → 5 на 600 при +40 %). Множитель идёт по `hpMult`, то есть заодно растит блок и лечение врага. Лимит Кровопускания бот не заметил (правка под живого игрока: кровь мимо блока и стакается, четыре пореза за ход снимали элиту).

v0.37 (блок: Плащ странника даёт блок каждый ход, Щитовой удар — полный кубик, 60/70/80 % в блок и толчок цели назад вместо сквозняка, Глухая оборона 4/6/8, Панцирь черепахи 3/4/5 DEF, Ответный удар 60/80/100 %) на 600, v0.36 → v0.37. Первая сигнатура: Воин 31.0 → 43.3 %, Маг 42.0 → 45.5, Ассасин 35.5 → 38.3, Паладин 33.7 → 37.7, Берсерк 30.8 → 32.2, Лучник 48.8 → 54.3. Вторая: Воин 35.0 → 40.3, Маг 36.3 → 38.2, Ассасин 29.2 → 34.0, Паладин 37.8 → 47.2, Берсерк 37.2 → 45.3, Лучник 42.2 → 45.0. Вклад каждой правки мерялся откатом одной из них на том же дереве: **плащ — +2.5…+7 всем** (главный рычаг пакета: блок каждый ход стоит примерно как `DEFEND_MULT`), Панцирь черепахи и Глухая оборона вместе — от −2.5 до +1.7 (ноль в шуме, единицу блока бот не видит), Щитовой удар — около +7.6 Воину (полный кубик вместо ×0.75, толчок бот почти не замечает — как и Крюк), Ответный удар 50 → 60 % — в шуме (на мече 4–6 обе доли округляются в 3). Пятеро вышли за коридор 20–40 (Воин 43/40, Маг 45 первой, Паладин 47 второй, Берсерк 45 второй, Лучник 54/45) — **правки сделаны по просьбе пользователя как есть, компенсация ждёт его решения**; кандидаты: числа плаща 2/3/4 за ход, `ACT_DMG_BONUS`, откат кубика Щитового удара.

v0.36 (переход фазы босса стоит хода под стражей `phase2.guard`, Ярость Вожака +1 вместо +2) на 600, v0.35.1 → v0.36: Воин 30.3 → 30.2 %, Маг 39.8 → 41.2, Ассасин 36.3 → 34.8, Паладин 31.7 → 32.0, Берсерк 28.0 → 30.2, Лучник 48.2 → 48.0 — стража съедает подарок свободного хода целиком, всё в шуме; Лучник выше коридора ещё с v0.35.1. Только Лес на трёх актах после Ярости +1 (`SIM_LOCS=forest,forest,forest`, 200): Воин 71, Маг 133, Ассасин 81, Паладин 89, Берсерк 64, Лучник 135 побед — Вожак второго акта всё равно главный убийца локации. Голый пропуск хода без стражи (отвергнут): Воин 37.3 %, Маг 47.8, Ассасин 49.8, Паладин 44.7, Берсерк 43.0, Лучник 55.8 — боссы второго акта были главным местом гибели, свободный ход стоил им 7–12 пунктов; с уроном врагов 1.3 / 1.5 / 1.75 поверх голого пропуска было 23.5 / 40.8 / 38.2 / 29.0 / 34.3 / 43.3. База v0.35.1 сама выше v0.35 (Воин 30, Маг 40, Ассасин 36, Паладин 32, Берсерк 28, Лучник 48 против 26 / 36 / 33 / 31 / 26 / 43): снятие двух слабых бронных артефактов подняло средний дроп на 3–5 пунктов.

v0.35 (мифический тир 4 в обычном пуле акта 2, `rareGear`) — самый резкий рычаг из найденных после урона врагов: 600 забегов, v0.34.2 → 5 % → 8 % → 15 %: Воин 23.2 → 26.0 → 27.7 → 34.0, Маг 33.8 → 36.5 → 38.7 → 42.5, Ассасин 31.0 → 33.2 → 34.5 → 38.7, Паладин 25.3 → 31.0 → 32.8 → 39.0, Берсерк 21.8 → 26.3 → 28.3 → 31.7, Лучник 38.0 → 43.2 → 47.3 → 53.2; взято 5 % (Лучник чуть выше коридора, он стоял у кромки и до этого). Тир 4 в акте 2 — это четвёртый сокет и кубик третьего акта к боссу второго, где бот гибнет чаще всего; если понадобится опустить кого-то — это первый кандидат на откат.

v0.34.2 (Пороховая мартышка: цикл `бросок, бросок, подрыв` и встреча «Мартышка + Пират + Крыса» вместо «две Мартышки + Пират» — по забегу из статистики: два Подрыва по 32 за один ход третьего акта не дефались ничем, кроме удара по площади) — только Корабль на трёх актах (`SIM_LOCS=ship,ship,ship`, 200), до → после: Воин 27 → 36, Маг 48 → 56, Ассасин 47 → 49, Паладин 47 → 60, Берсерк 15 → 23, Лучник 28 → 47 побед; гибели ушли с обычных боёв 4–5 второго акта на элиту 8 и боссов. Корабль и после этого самая тяжёлая локация для бота (Пещеры 20 / 55 / 59 / 36 / 25 / 55, Улей 65 / 141 / 81 / 75 / 73 / 158).

v0.34.1 (Сердце улья: Кислотный дождь Горение 3 → 2) — только Улей на трёх актах (`SIM_LOCS=hive,hive,hive`, 200): Воин 57 → 65, Маг 123 → 141, Ассасин 67 → 81, Паладин 66 → 75, Берсерк 56 → 73, Лучник 132 → 158 побед; Улей для бота мягче Пещер (20 / 55 / 59 / 36 / 25 / 55), гибели там почти все на боссе. Отвергнуто: лимит Феромона роя — 0 пунктов (босс до третьего не доживает), одна оса вместо двух — шум, Горение 2 с Шипами 2 → 1 — 87 / 150 / 104 / 101 / 84 / 158 (ближним слишком много, шипы оставлены до плейтеста). Полный SIM на 600 после правки: Воин 23.2 %, Маг 33.7, Ассасин 28.8, Паладин 24.8, Берсерк 20.2, Лучник 35.3 (v0.33.1: 21.5 / 32.7 / 26.8 / 23.8 / 20.0 / 34.5) — сдвиг +1…+2, в шуме.

v0.34 («Ответный удар» вместо «Натиска» у Воина: пассивка 50/75/100 % среднего урона оружия ударившему, когда блок гасит удар) — Воин со вторым артефактом на 600 (v0.33.1 как база): Натиск 19.8 % → Ответный удар 28.7 %; бот бьёт в ответ сам, потому что планирует ход через `resolveEnemyTurn` на копии, и ценит пассивку в `artifactValueRaw`.

v0.33.2 (Боевой транс на половине HP с гашением удара 1/2/3) — Берсерк со вторым артефактом на 600: ⅔ без гашения 27.5 %, половина с 1/2/3 — 30.0, 2/2/3 — 32.0, 2/3/4 — 39.2, 3/3/4 — 41.5; взято 1/2/3.

v0.33.1 (акт 2 `ACT_DMG_BONUS` 1.45 → 1.4, взрыв Лавового слизня 12 → 8 — по статистике живых игроков: 12 из 20 гибелей во втором акте, 0 в третьем) на 600 забегах, до → после: Воин 19.5 → 21.5 %, Маг 28.3 → 32.7, Ассасин 27.0 → 26.8, Паладин 21.0 → 23.8, Берсерк 19.5 → 20.0, Лучник 31.5 → 34.5 — все в коридоре. Только Пещеры (200, `SIM_LOCS=caves,caves,caves`): Воин 11 → 20, Маг 46 → 55, Ассасин 55 → 59, Паладин 29 → 36, Берсерк 22 → 25, Лучник 59 → 55. В записях статистики `detail.battles` хранит только название, исход и ходы боя — урона по боям нет, так что «сколько получаем в каком акте» из таблицы пока не посчитать.

v0.33 (вторые персональные артефакты) на 300 забегах, первая → вторая сигнатура: Воин 17 → 18 %, Маг 30 → 25, Ассасин 25 → 23, Паладин 21 → 24, Берсерк 21 → 27, Лучник 32 → 27 (600 на второй — см. GDD §13). Что не сработало и почему: Натиск за 1 STA не окупается никогда (два удара на 0.9 слабее трёх на 0.75) — сделан за 0 STA; Боевой транс как «+2/3/4 к Силе ниже половины HP» ронял Берсерка до 4 % (без Ярости он теряет лишние действия, а Сила ниже половины приходит, когда бой уже проигран): +3/4/5 Силы 8 %, порог 75 % 5 %, Сила по ступеням недостающих HP 14 %, +1 STA ниже половины 11 %, и только «+3/4/5 Силы и +1 STA ниже ⅔ HP» — 24 % (ниже 60 % с +4/5/6 — 30 %); Двойной выпад 60/70/80 % — 18 %, 70/80/90 — 20, КД 1 — 22, 75/85/100 — 23 (взято); Ореол возмездия с Регенерацией 1/1/2 — 18 %, Шипы 3/4/5 — 21, КД 2 — 23, Регенерация 2/2/3 — 24 (взято). Бот ценит новые вещи в `artifactValueRaw` (`onslaught`, `thorns` на себя, `lowHpStr`/`lowHpSta`) и Силу транса в `dpt` — иначе выбрасывал сигнатуру при первой находке.

v0.32.3 (Дымовая шашка 2 STA / КД 4 вместо 3/3/2 STA / КД 3, Тень покрова 1 ход вместо 2, сроки скрытности складываются) на 600 забегах: Ассасин 21 → 27 %, остальные ±1 — все в коридоре.

v0.31.1 (пять бронных артефактов: Каменная кожа, Печать стойкости, Оберег панциря, Амулет уклонения, Глухая оборона) на 600 забегах, v0.31 → v0.31.1: Воин 17 → 19 %, Маг 29 → 28, Ассасин 23 → 23, Паладин 23 → 22, Берсерк 17 → 19, Лучник 36 → 33 — шум ±3. Пассивки на статах перков брони бот берёт по весам в `artifactValueRaw` (hitReduce ×5, defendBonus ×2.5, blockKeep ×1.5, dodgeStart ×5).

v0.31 (типы сокетов) на 600 забегах, v0.30.2 → v0.31: Воин 21 → 17 %, Маг 35 → 29, Ассасин 29 → 23, Паладин 26 → 23, Берсерк 23 → 16, Лучник 33 → 36. Механика стоит боту 3–7 пунктов, Воин и Берсерк ниже коридора 20–40 — **оставлено без компенсации до плейтеста (решение пользователя, вариант A)**: бот сокеты не планирует, живой игрок — да. Замеренные рычаги на будущее: `SLOT_ANY_CHANCE` 0.5/0.6 — только Берсерк +4 (16 → 20), остальные ±1; универсальные стартовые сокеты — Ассасин +4, Паладин +2, Воин/Маг/Берсерк 0. Воина ни один из них не двигает — если понадобится, его рычаги те же, что в v0.30.1 (усталость, урон врагов). Лаборатория — копии дерева в scratchpad с `sed` по константе, четыре SIM параллельно.

v0.30.1 (Воин: своя усталость 0.75 и сквозной Щитовой удар 30/40/50 %; плюс четыре правки артефактов) на 600 забегах: Воин 16 → 21 %, Маг 35, Ассасин 29, Паладин 26, Берсерк 23, Лучник 33. Сам воинский пакет дал Воину 16 → 20 при пятерых без движения; остальное — от четырёх правок: шипы перестали бить мимо блока (`kind: 'thorns'` вычитается из блока в `damageEnemy`/`damageHero`, раны по-прежнему мимо) — это заметный подарок тем, кто держит блок против жука и колосса (Берсерк +4), Искра 4/6/8 → 3/5/6, Флакон яда получил `usesPerTurn` 1/2/2, Цепная молния 4 → 3 MP; три нерфа бот почти не заметил. Рычаги только на герое — общую `DEFAULT_FATIGUE` не трогали: она давала Воину те же +2, но двигала пятерых чужих (Маг −3, Лучник +6). Сквозняк приёма — поле `splashPct` у эффекта `attack`, со `splash` копья брался максимум, не сумма (поле убрано в v0.37 вместе со сквозняком Щитового удара: сквозной удар остался только у копья). Замерено и отвергнуто: сквозной удар без усталости — 0 пунктов и без масштаба (30 % и 60 % одинаково), шипы на Щитовой удар — вдвое сильнее (25 % на 2/3/4 и 28 % на 3/5/7), но не легли в концепцию приёма (решение пользователя). Воин на 20 % был ниже тогдашнего коридора 30–50; в конце v0.30.1 коридор опущен до 20–40, и по нему все шестеро внутри (Воин 21, Берсерк 23, Паладин 26, Ассасин 29, Лучник 33, Маг 35).

v0.30 (вторые фазы боссов, слиты на v0.29 без перетюна — решение пользователя) на 600 забегах, main → с фазами: Воин 18 → 16, Маг 41 → 35, Ассасин 38 → 27, Паладин 35 → 25, Берсерк 30 → 20, Лучник 39 → 34. Фазы стоят боту 2–11 пунктов, смерти в основном на боссах (R10). Четверо были ниже тогдашнего коридора 30–50; с коридором 20–40 вопрос снят, но просадка от фаз всё равно ждёт живого плейтеста.

v0.26–0.26.1 (дальность) на 600 забегах: итог — Воин 38 %, Маг 32 %, Ассасин 40 %, Паладин 48 %, Берсерк 51 %, Лучник 44 % (база v0.25: 42 / 29 / 51 / 52 / 53 / 46; в v0.26 с плетью, у которой ещё был перк Слабости по всему ряду, было 39 / 31 / 42 / 52 / 54 / 44 — перк стоил 2–4 пункта). Строго «первый в ряду» с призывом в хвост роняло ближних героев на 8–11 пунктов (Воин 33.5 %, Ассасин 39.7 %), с призывом вперёд — на 2–5; два ряда со строем врагов — бесплатно (отвергнуто как незаметное). Рычаги компенсации ближнему оружию (отложены до плейтеста): +1 Сила свойством типа — перебор (+10…+15 пунктов, Берсерк 65 %), +1 к максимуму урона — Воин 40 %, урон врагов 1.2 / 1.4 / 1.5 — все в коридоре (Воин 47.5 %, Паладин 60 %). Исключения (Молот света, копьё) и новые плеть с крюком бот почти не замечает (±3). Статические надбавки боту за дальность вредят (3–8 пунктов) — бот видит дальность перебором ходов, ценить её в статике не надо. Полные цифры — GDD §13. По новому коридору 30–50 % все шестеро внутри, Берсерк у верхней кромки — компенсация ближнему оружию не нужна, если и понадобится рычаг, то в сторону сложности. Бот берёт плеть охотно: коэффициент её кубика в gearGain — рычаг на ±2 пункта у ближних. В облачной сессии лаборатория — `runlab.py` в scratchpad: копия `src`+`tests`+конфигов на вариант, патчи констант строками, четыре варианта параллельно на четырёх ядрах, 600 забегов × 6 героев ≈ 2 мин на вариант.

v0.29 (событие «Вор», 5 % + 5 % клеток события) на 600 забегах, до → после: Воин 21 → 18 %, Маг 43 → 41 %, Ассасин 37 → 38 %, Паладин 34 → 35 %, Берсерк 34 → 30 %, Лучник 40 → 39 % — налог в 1–4 пункта на ближних, в пределах шума ±4, но Берсерк лёг на нижнюю кромку коридора, Воин и так ниже. Бот убивает деньгокрада в 12–14 % боёв Воином и 16–18 % Паладином против 42–53 % Магом, Берсерком и Лучником (`gnome-odds`): для ближних с усталостью 0.7 вор — почти чистый налог, рычаги — веса события, `evade` 50 и `EVADE_DROP` 12.

Состояние до вора (v0.28, нерфы под живого игрока: Камень силы 2/3/4, аффиксы на ступень ниже с 3-го тира, Ярость 2/2/3, шашка 1/2/2, стрела 3/4/5, Сглаз КД 3, Второе дыхание 1/2/3, `DEFAULT_FATIGUE` 0.7, своя усталость Берсерка 0.8): на 600 забегах Воин 21 %, Маг 43 %, Ассасин 37 %, Паладин 34 %, Берсерк 34 % (39 % до своей усталости 0.8), Лучник 40 % (до пакета на том же боте 37 / 51 / 52 / 47 / 58 / 48). Воин ниже коридора 30–50 — ждёт компенсации (своя усталость 0.8 или +1 к максимуму ближнего оружия, см. GDD §13 v0.26/v0.28). Бот с v0.28 подправлен под живого игрока и это его текущая версия: тир предмета 4 очка вместо 10 и чужое оружие −8 в `gearGain`, скрытность ×10 в оценке статусов, персональный артефакт ×2 в `artifactValue` — старые цифры ниже сняты на прежнем боте и с новыми напрямую не сравниваются. Лаборатория нерфов — `runlab.py` (см. выше): базой брать `bot3` = текущий бот репо без игровых патчей. Состояние до этого (v0.23, сверка приёмов врагов): на 600 забегах Воин 42 %, Маг 28 %, Ассасин 46 %, Паладин 52 %, Берсерк 53 %, Лучник 47 %; пятеро в коридоре, Маг ниже с v0.19.1. Ловушка, найденная здесь: статусы `thorns` и `dodge` у врага складываются (`STACKING` в combat.ts) и висят до конца боя (`turns: -1`), поэтому приём, который вешает их в цикле, раздувает защиту без предела и бот перестаёт бить такого врага — паты Воина 9 → 57 на 600 забегах на шипах Кладки, паты Ассасина 4 → 13 на уклонении восьми врагов. Лечится условием у приёма (`noThorns` / `noDodge` в enemies.ts — «такого статуса ещё нет»): цикл ИИ пропускает приём с невыполненным условием и берёт следующий. Условие на уклонение убрало паты 27 → 8 суммарно, не сдвинув проценты побед. Любой новый приём врага, который вешает складывающийся бессрочный статус на себя, обязан нести такое условие. До эффектов при смерти (v0.21, крит как пара статов): на 600 забегах Воин 48 %, Маг 32 %, Ассасин 51 %, Паладин 60 %, Берсерк 57 %, Лучник 49 %; `ACT_DMG_BONUS` снижен до 1.25 / 1.45 / 1.6, чтобы компенсировать отказ от крита ×2 (он стоил 5–11 пунктов каждому). Крит. урон бота почти не двигает: +20 % всем героям дало 0 пунктов, шансы низкие и крит-билд бот не собирает. До крит-реформы на тех же 600: Воин 42 %, Маг 30 %, Ассасин 37 %, Паладин 52 %, Берсерк 54 %, Лучник 43 %. До правки владения на тех же 600 было 42 / 28 / 38 / 51 / 53 / 42 %: само бинарное владение стоило 2–7 пунктов, подъём веса дропа своего типа (40 → 60 против 20, `WEAPON_DROP_WEIGHT`) вернул их обратно — это заодно рабочий рычаг на +3…+7 пунктов. Ниже коридора Маг и Ассасин (просели ещё на неизмеренных нерфах v0.19.1) — подъём отдельной задачей. v0.18 (Щитовой удар — блок долей урона, Таран): на 300 забегах Воин 45 %, Маг 43 %, Ассасин 45 %, Паладин 52 %, Берсерк 54 %, Лучник 47 %. v0.15 (статы экипировки по типу брони и весу оружия) на 1200 забегах: Воин 49 %, Маг 40 % (мана 7, стрела до 3/4/5 раз за ход), Ассасин 44 %, Паладин 57 %, Берсерк 52 %, Лучник 47 %. DEF лёгкой брони — сильный рычаг для Мага, Ассасина и Лучника (−1 на тирах 2–5 стоил Ассасину 7 пунктов); усиление перков лёгкой брони бот не замечает, +1 DEF героя — замечает. На 300 забегах разброс ±5, решения принимать по 600. Число событий на этаж — тоже рычаг (третье событие дало +7–12 пунктов). Лаборатория для экспериментов: scratchpad `lab2.py` (копия дерева + патчи + SIM). Паты Паладина ~1–3 % против блокирующей элиты — открытая задача.

Сравнение «до/после» честно только на одной версии бота: `git worktree add --detach <scratchpad>/base HEAD`, node_modules — junction через PowerShell `New-Item -ItemType Junction`, прогнать SIM там. Убирать строго: сначала `(Get-Item …\node_modules).Delete()`, потом `git worktree remove --force`. В облачной сессии проще: `cp -r src tests` плюс конфиги в scratchpad, `ln -s` на node_modules, патчить и гнать SIM там (rsync в контейнере нет).

Перебор рычагов: скрипт в scratchpad патчит одну строку исходника, гонит `SIM_N=200`, откатывает. Python в консоли — `PYTHONIOENCODING=utf-8`.

## Стиль кода

- TS strict, `noUnusedLocals/Parameters`. Одинарные кавычки, точки с запятой, 2 пробела, длинные строки допустимы (до ~220).
- Комментарии и JSDoc — по-русски, объясняют «почему» и игровой смысл, не «что». Числа в комментариях сверять с GDD.
- Секции файлов — `// ─── Название ───…`, в enemies.ts локации — `// ═══`.
- Данные: `const list: XxxDef[] = [...]` → `export const XXX = Object.fromEntries(...)`, `xxxDef(id)` бросает на неизвестный id.
- UI без фреймворка: `h('div', { class: 'x' }, ...children)`, экран целиком перерисовывается через `app.render()`; единственное исключение — наведение (preview.ts, diff.ts, таймер в топбаре), которое пишет в готовые узлы.

## Git

- Ветки `feat/…`, `ui/…`, `fix/…`; после работы merge в `main` (без squash, история — merge-коммиты). Remote: github.com/aleasdeck/AldkMonsterVersusGame.
- Коммиты на английском в стиле `feat: …`, `ui: …`, `fix(ui): …`. **Без AI-атрибуции** (правило пользователя).
- На рабочей машине fetch/push идут через корпоративный прокси: `HTTPS_PROXY` брать из окружения / локальных заметок, в репо не писать; push лучше в фоне (GCM может открыть окно входа).
- Стейл `.git/index.lock` от прошлой сессии — удалять, если `tasklist` не показывает git.

## Ловушки тулинга

- `npx tsc` в цепочке `&&` может подхватить древний глобальный TypeScript (сотни ошибок про `import type`). Только `node node_modules/typescript/bin/tsc`.
- Длинные heredoc (~150 строк с кавычками) в Bash-туле падают с «unexpected EOF». Большие скрипты — через Write в scratchpad, потом `python file.py`.
- `npm test` пропускает SIM-тесты (skipIf без `SIM`), долгий SIM — свой таймаут 30 мин на `it`.
- Бот выбирает оружие через `weaponDice(def, gear)`, а не сырой dmgMin/dmgMax — иначе хватает чужое оружие.
- Кнопка с атрибутом `disabled` не получает `mouseenter` — плитки и строка зелья вместо него используют класс `off` и проверку в onclick, иначе ридаут не покажет причину.
- В JS-регэкспах `` и `\w` не знают кириллицы: границы слов в `keywords.ts` — через lookaround `(?<![а-яёa-z])`.
- Шрифт Handjet мельче VT323: размеры ниже 14 px нечитаемы, база 23 px; заголовки Tiny5.

## Приоритеты пользователя

- Ядро игры — пошаговый бой и сборка билда через оружие/броню со слотами. Визуал не приоритет, процедурные спрайты устраивают.
- Не добавлять без запроса: рюкзак, мета-прогрессию силы, ветвящуюся карту, звук, новые траты золота.
- Пользователь любит получать 2–4 варианта с рекомендацией и выбирать («давай A»). Балансовые правки — с цифрами SIM до/после.
